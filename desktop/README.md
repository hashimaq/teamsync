# TeamSync Desktop (Tauri)

Starter package only. No UI yet.

## Next steps

```bash
cd desktop
npm create tauri-app@latest
```

Point the Tauri webview at the Next.js `web` app or a dedicated desktop shell, and reuse `@teamsync/shared` + Supabase.
