# TeamSync Mobile (Expo)

Starter package only. No UI yet.

## Next steps

```bash
cd mobile
npx create-expo-app@latest . --template blank-typescript
```

Share types via `@teamsync/shared` and connect to the same Supabase project as `web/` and `extension/`.
