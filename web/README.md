# TeamSync Web

Next.js App Router application (moved from the original repo root).

```bash
# from monorepo root
npm run dev:web
```

Ensure `web/.env` exists with:

```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```
